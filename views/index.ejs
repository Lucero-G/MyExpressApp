<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <div class="container">
      <h1><%= title %></h1>
      <p>Bienvenido a <%= title %>. Estamos encantados de tenerte aquí. Explora nuestra aplicación y descubre todas las increíbles características que tenemos para ofrecerte.</p>
      <a href="#" class="btn" id="exploreBtn">Explorar</a>
  </div>

    <div class="custom-alert" id="customAlert">
      ¡Gracias por tu interés! Pronto podrás explorar más.
    </div>

    <script>
      document.getElementById('exploreBtn').addEventListener('click', function(e) {
          e.preventDefault();
          showCustomAlert();
      });

      function showCustomAlert() {
          const alert = document.getElementById('customAlert');
          alert.classList.add('show');
          setTimeout(() => {
              alert.classList.remove('show');
          }, 3000);
      }
    </script>
  </body>
</html>
